import Layout from '../../components/Layout';
import Section from '../../components/Section';

const DashboardIndex = () => {
    return (
        <Layout>
            <Section>
                <div className="pg-dashboard">dashboard index</div>
            </Section>
        </Layout>
    );
};

export default DashboardIndex;
